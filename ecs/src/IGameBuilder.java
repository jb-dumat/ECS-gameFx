/**
 * An interface IGameBuilder
 * The game contains the method to build
 * a game.
 * In this project a simple class implementing this
 * is defined and builds statically the game. If you wish
 * to add complexity to the process you just need to implement this and
 * change the build logic.
 */
public interface IGameBuilder {
    void buildGame();
}
